[Unit]
StartLimitIntervalSec=300
StartLimitBurst=20

[Service]
Restart=on-failure
RestartSec=3

# The Python bridge waits for a backend that returns a parseable default-sink
# volume. An ExecStartPre exit-code probe is insufficient because wpctl can
# briefly return success with empty output while the audio graph is starting.
